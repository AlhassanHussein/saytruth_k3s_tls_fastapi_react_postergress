  apiVersion: v1
  kind: Service
  metadata:
    name: saytruth-service
  spec:
    selector:
      app: saytruth-api
    ports:
      - protocol: TCP
        port: 8000
        targetPort: 8000
    type: ClusterIP
